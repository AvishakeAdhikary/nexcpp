[package]
name = "{{ module_name | replace('_', '-') }}"
version = "0.1.0"
edition = "2021"
description = "Rust cxx bindings for {{ header_basename }}"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
