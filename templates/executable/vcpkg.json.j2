{
    "name": "{{ name | replace('_', '-') }}",
    "version": "0.1.0",
    "description": "{{ description }}",
    "license": "MIT",
    "dependencies": [
{% for d in dependencies %}        "{{ d }}"{% if not loop.last %},{% endif %}
{% endfor %}    ]
}
