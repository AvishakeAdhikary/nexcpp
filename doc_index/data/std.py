"""Bundled C++ standard library reference entries for nexcpp.

All content in this module is original work, released under the MIT License
along with the rest of nexcpp. Standard library symbol names, function
signatures, and version information are facts about the C++ public API and
are not copyrightable. All prose descriptions and code examples in this file
are original; they do not derive from external reference sites or other
copyrighted sources.
"""

from __future__ import annotations

from ._common import e

_URL = "https://en.cppreference.com/w/cpp/"

ENTRIES = [
    # ---------------------------------------------------------------- containers
    e(
        "std::vector",
        header="<vector>",
        since="C++98",
        brief="Owning, heap-allocated dynamic array whose capacity grows on demand. Random access is O(1); push_back is amortized O(1).",
        signature="template<class T, class Alloc = std::allocator<T>> class vector;",
        example="std::vector<int> v{1, 2, 3};\nv.push_back(4);\nint x = v[0];",
        url=_URL + "container/vector",
        source="std",
    ),
    e(
        "std::array",
        header="<array>",
        since="C++11",
        brief="Fixed-size contiguous sequence whose length is part of its type. Stored on the stack like a C array but with container semantics.",
        signature="template<class T, std::size_t N> struct array;",
        example="std::array<int, 3> a{1, 2, 3};\nint x = a[2];",
        url=_URL + "container/array",
        source="std",
    ),
    e(
        "std::deque",
        header="<deque>",
        since="C++98",
        brief="Double-ended queue with O(1) insertion at both ends and O(1) random access. Internally a sequence of fixed-size blocks.",
        signature="template<class T, class Alloc = std::allocator<T>> class deque;",
        example="std::deque<int> d;\nd.push_back(1);\nd.push_front(0);",
        url=_URL + "container/deque",
        source="std",
    ),
    e(
        "std::list",
        header="<list>",
        since="C++98",
        brief="Doubly-linked list with O(1) splice and O(1) insertion or erasure given an iterator. No random access.",
        signature="template<class T, class Alloc = std::allocator<T>> class list;",
        example="std::list<int> l{1, 2, 3};\nl.push_front(0);",
        url=_URL + "container/list",
        source="std",
    ),
    e(
        "std::forward_list",
        header="<forward_list>",
        since="C++11",
        brief="Singly-linked list; lower memory overhead than std::list but only forward iteration.",
        signature="template<class T, class Alloc = std::allocator<T>> class forward_list;",
        example="std::forward_list<int> fl{1, 2, 3};\nfl.push_front(0);",
        url=_URL + "container/forward_list",
        source="std",
    ),
    e(
        "std::map",
        header="<map>",
        since="C++98",
        brief="Sorted associative container of unique keys, typically a red-black tree. Lookup, insert, and erase are O(log n).",
        signature="template<class K, class V, class Cmp = std::less<K>, class Alloc = std::allocator<std::pair<const K, V>>> class map;",
        example="std::map<std::string, int> m;\nm[\"a\"] = 1;",
        url=_URL + "container/map",
        source="std",
    ),
    e(
        "std::unordered_map",
        header="<unordered_map>",
        since="C++11",
        brief="Hash table mapping unique keys to values with average O(1) lookup, insert, and erase.",
        signature="template<class K, class V, class Hash = std::hash<K>, class Eq = std::equal_to<K>, class Alloc = std::allocator<std::pair<const K, V>>> class unordered_map;",
        example="std::unordered_map<std::string, int> m;\nm[\"a\"] = 1;",
        url=_URL + "container/unordered_map",
        source="std",
    ),
    e(
        "std::set",
        header="<set>",
        since="C++98",
        brief="Sorted set of unique keys backed by a balanced binary tree. Iteration walks elements in key order.",
        signature="template<class K, class Cmp = std::less<K>, class Alloc = std::allocator<K>> class set;",
        example="std::set<int> s{3, 1, 2};\ns.insert(4);",
        url=_URL + "container/set",
        source="std",
    ),
    e(
        "std::unordered_set",
        header="<unordered_set>",
        since="C++11",
        brief="Hash-based set of unique keys with average O(1) membership checks.",
        signature="template<class K, class Hash = std::hash<K>, class Eq = std::equal_to<K>, class Alloc = std::allocator<K>> class unordered_set;",
        example="std::unordered_set<int> s{1, 2, 3};\nbool has = s.count(2) > 0;",
        url=_URL + "container/unordered_set",
        source="std",
    ),
    e(
        "std::multimap",
        header="<map>",
        since="C++98",
        brief="Sorted associative container that allows multiple values per key.",
        signature="template<class K, class V, class Cmp = std::less<K>> class multimap;",
        example="std::multimap<int, std::string> m;\nm.insert({1, \"a\"});\nm.insert({1, \"b\"});",
        url=_URL + "container/multimap",
        source="std",
    ),
    e(
        "std::multiset",
        header="<set>",
        since="C++98",
        brief="Sorted set that retains duplicate keys.",
        signature="template<class K, class Cmp = std::less<K>> class multiset;",
        example="std::multiset<int> s{1, 1, 2};",
        url=_URL + "container/multiset",
        source="std",
    ),
    e(
        "std::stack",
        header="<stack>",
        since="C++98",
        brief="LIFO container adaptor; by default wraps std::deque and exposes push, pop, top.",
        signature="template<class T, class Container = std::deque<T>> class stack;",
        example="std::stack<int> s;\ns.push(1);\nint x = s.top();",
        url=_URL + "container/stack",
        source="std",
    ),
    e(
        "std::queue",
        header="<queue>",
        since="C++98",
        brief="FIFO container adaptor; by default wraps std::deque and exposes push, pop, front, back.",
        signature="template<class T, class Container = std::deque<T>> class queue;",
        example="std::queue<int> q;\nq.push(1);\nint x = q.front();",
        url=_URL + "container/queue",
        source="std",
    ),
    e(
        "std::priority_queue",
        header="<queue>",
        since="C++98",
        brief="Max-heap container adaptor that always exposes its largest element via top().",
        signature="template<class T, class Container = std::vector<T>, class Cmp = std::less<typename Container::value_type>> class priority_queue;",
        example="std::priority_queue<int> pq;\npq.push(3);\npq.push(5);\nint top = pq.top();",
        url=_URL + "container/priority_queue",
        source="std",
    ),
    # ---------------------------------------------------------------- strings
    e(
        "std::string",
        header="<string>",
        since="C++98",
        brief="Owning, mutable, dynamically sized sequence of char. Alias for std::basic_string<char>.",
        signature="using string = std::basic_string<char>;",
        example="std::string s = \"hello\";\ns += \", world\";",
        url=_URL + "string/basic_string",
        source="std",
    ),
    e(
        "std::string_view",
        header="<string_view>",
        since="C++17",
        brief="Non-owning, read-only view of a contiguous run of characters. Avoids copies when passing string data.",
        signature="using string_view = std::basic_string_view<char>;",
        example="void log(std::string_view s) {}\nlog(\"literal\");",
        url=_URL + "string/basic_string_view",
        source="std",
    ),
    e(
        "std::stringstream",
        header="<sstream>",
        since="C++98",
        brief="In-memory stream that supports both reading and writing formatted text. Useful for ad-hoc parsing or building strings.",
        signature="using stringstream = std::basic_stringstream<char>;",
        example="std::stringstream ss;\nss << \"x=\" << 42;\nstd::string out = ss.str();",
        url=_URL + "io/basic_stringstream",
        source="std",
    ),
    e(
        "std::format",
        header="<format>",
        since="C++20",
        brief="Type-safe string formatting based on a Python-like brace-replacement mini-language.",
        signature="template<class... Args> std::string format(std::format_string<Args...> fmt, Args&&... args);",
        example="auto s = std::format(\"{} {}\", \"hi\", 42);",
        url=_URL + "utility/format/format",
        source="std",
    ),
    e(
        "std::print",
        header="<print>",
        since="C++23",
        brief="Formatted printing that writes directly to stdout (or a chosen FILE*) without going through iostreams.",
        signature="template<class... Args> void print(std::format_string<Args...> fmt, Args&&... args);",
        example="std::print(\"x = {}\\n\", 42);",
        url=_URL + "io/print",
        source="std",
    ),
    # ---------------------------------------------------------------- utility
    e(
        "std::optional",
        header="<optional>",
        since="C++17",
        brief="Wraps a value that may or may not be present. Acts as a nullable type without separate sentinel values.",
        signature="template<class T> class optional;",
        example="std::optional<int> x = 42;\nif (x) std::cout << *x;",
        url=_URL + "utility/optional",
        source="std",
    ),
    e(
        "std::variant",
        header="<variant>",
        since="C++17",
        brief="Tagged-union type that holds exactly one of a fixed set of alternative types.",
        signature="template<class... Types> class variant;",
        example="std::variant<int, std::string> v = 42;\nv = \"hi\";",
        url=_URL + "utility/variant",
        source="std",
    ),
    e(
        "std::expected",
        header="<expected>",
        since="C++23",
        brief="Discriminated value that holds either a result or an error, providing a structured alternative to exceptions.",
        signature="template<class T, class E> class expected;",
        example="std::expected<int, std::string> parse() { return 42; }",
        url=_URL + "utility/expected",
        source="std",
    ),
    e(
        "std::any",
        header="<any>",
        since="C++17",
        brief="Type-erased container for a single value of any copyable type, retrievable via std::any_cast.",
        signature="class any;",
        example="std::any a = 42;\nint x = std::any_cast<int>(a);",
        url=_URL + "utility/any",
        source="std",
    ),
    e(
        "std::tuple",
        header="<tuple>",
        since="C++11",
        brief="Fixed-size, heterogeneous collection of values, accessed by index via std::get.",
        signature="template<class... Types> class tuple;",
        example="auto t = std::make_tuple(1, \"x\", 3.14);\nint x = std::get<0>(t);",
        url=_URL + "utility/tuple",
        source="std",
    ),
    e(
        "std::pair",
        header="<utility>",
        since="C++98",
        brief="Two-element struct with members .first and .second. Commonly returned by associative container insertions.",
        signature="template<class T1, class T2> struct pair;",
        example="auto p = std::make_pair(1, \"x\");",
        url=_URL + "utility/pair",
        source="std",
    ),
    e(
        "std::span",
        header="<span>",
        since="C++20",
        brief="Non-owning view over a contiguous block of elements with optional compile-time extent.",
        signature="template<class T, std::size_t Extent = std::dynamic_extent> class span;",
        example="void sum(std::span<const int> s) {}\nstd::vector<int> v{1,2,3};\nsum(v);",
        url=_URL + "container/span",
        source="std",
    ),
    e(
        "std::byte",
        header="<cstddef>",
        since="C++17",
        brief="Distinct unsigned type for raw memory bytes that does not implicitly convert to integer arithmetic types.",
        signature="enum class byte : unsigned char {};",
        example="std::byte b{0xFF};\nb <<= 1;",
        url=_URL + "types/byte",
        source="std",
    ),
    e(
        "std::source_location",
        header="<source_location>",
        since="C++20",
        brief="Captures the source file, line, column, and function name of the call site, replacing __FILE__ / __LINE__ idioms.",
        signature="struct source_location;",
        example="void log(std::source_location loc = std::source_location::current()) {}",
        url=_URL + "utility/source_location",
        source="std",
    ),
    # ---------------------------------------------------------------- memory
    e(
        "std::unique_ptr",
        header="<memory>",
        since="C++11",
        brief="Smart pointer that owns its target exclusively and deletes it on destruction. Move-only.",
        signature="template<class T, class Deleter = std::default_delete<T>> class unique_ptr;",
        example="auto p = std::make_unique<int>(42);",
        url=_URL + "memory/unique_ptr",
        source="std",
    ),
    e(
        "std::shared_ptr",
        header="<memory>",
        since="C++11",
        brief="Reference-counted smart pointer that allows multiple owners. The target is destroyed when the last shared_ptr is released.",
        signature="template<class T> class shared_ptr;",
        example="auto p = std::make_shared<int>(42);\nauto q = p;",
        url=_URL + "memory/shared_ptr",
        source="std",
    ),
    e(
        "std::weak_ptr",
        header="<memory>",
        since="C++11",
        brief="Non-owning observer of a shared_ptr-managed object. Use .lock() to access if the target is still alive.",
        signature="template<class T> class weak_ptr;",
        example="std::weak_ptr<int> w = p;\nif (auto s = w.lock()) {}",
        url=_URL + "memory/weak_ptr",
        source="std",
    ),
    e(
        "std::make_unique",
        header="<memory>",
        since="C++14",
        brief="Constructs an object on the heap and wraps it in a unique_ptr in a single allocation.",
        signature="template<class T, class... Args> std::unique_ptr<T> make_unique(Args&&... args);",
        example="auto p = std::make_unique<std::vector<int>>(10);",
        url=_URL + "memory/unique_ptr/make_unique",
        source="std",
    ),
    e(
        "std::make_shared",
        header="<memory>",
        since="C++11",
        brief="Constructs an object and its shared control block together, saving an allocation versus shared_ptr<T>(new T).",
        signature="template<class T, class... Args> std::shared_ptr<T> make_shared(Args&&... args);",
        example="auto p = std::make_shared<int>(42);",
        url=_URL + "memory/shared_ptr/make_shared",
        source="std",
    ),
    # ---------------------------------------------------------------- function
    e(
        "std::function",
        header="<functional>",
        since="C++11",
        brief="Type-erased polymorphic wrapper around any callable matching a target signature.",
        signature="template<class R, class... Args> class function<R(Args...)>;",
        example="std::function<int(int)> f = [](int x) { return x * 2; };",
        url=_URL + "utility/functional/function",
        source="std",
    ),
    e(
        "std::move",
        header="<utility>",
        since="C++11",
        brief="Casts an expression to an rvalue reference so that a move constructor or move assignment can be selected.",
        signature="template<class T> constexpr std::remove_reference_t<T>&& move(T&& t) noexcept;",
        example="std::string a = \"hi\";\nstd::string b = std::move(a);",
        url=_URL + "utility/move",
        source="std",
    ),
    e(
        "std::forward",
        header="<utility>",
        since="C++11",
        brief="Perfect-forwarding cast used in templates to preserve the value category of a forwarding reference.",
        signature="template<class T> constexpr T&& forward(std::remove_reference_t<T>& t) noexcept;",
        example="template<class T> void wrap(T&& x) { inner(std::forward<T>(x)); }",
        url=_URL + "utility/forward",
        source="std",
    ),
    e(
        "std::swap",
        header="<utility>",
        since="C++98",
        brief="Exchanges two values, typically using move semantics. Many types provide an ADL-found overload for efficiency.",
        signature="template<class T> void swap(T& a, T& b) noexcept(/* see notes */);",
        example="int x = 1, y = 2;\nstd::swap(x, y);",
        url=_URL + "algorithm/swap",
        source="std",
    ),
    # ---------------------------------------------------------------- algorithms
    e(
        "std::sort",
        header="<algorithm>",
        since="C++98",
        brief="Sorts a random-access range in ascending order by default, using introsort. Comparator can be supplied as a third argument.",
        signature="template<class It> void sort(It first, It last);",
        example="std::vector<int> v{3, 1, 2};\nstd::sort(v.begin(), v.end());",
        url=_URL + "algorithm/sort",
        source="std",
    ),
    e(
        "std::find",
        header="<algorithm>",
        since="C++98",
        brief="Linear search for the first element equal to a given value; returns an iterator to the match or end().",
        signature="template<class It, class T> It find(It first, It last, const T& value);",
        example="auto it = std::find(v.begin(), v.end(), 2);",
        url=_URL + "algorithm/find",
        source="std",
    ),
    e(
        "std::copy",
        header="<algorithm>",
        since="C++98",
        brief="Copies elements from an input range into an output iterator.",
        signature="template<class In, class Out> Out copy(In first, In last, Out out);",
        example="std::vector<int> dst(v.size());\nstd::copy(v.begin(), v.end(), dst.begin());",
        url=_URL + "algorithm/copy",
        source="std",
    ),
    e(
        "std::transform",
        header="<algorithm>",
        since="C++98",
        brief="Applies a unary function to each element of a range, writing the result to an output iterator.",
        signature="template<class In, class Out, class F> Out transform(In first, In last, Out out, F op);",
        example="std::transform(v.begin(), v.end(), v.begin(), [](int x){ return x * x; });",
        url=_URL + "algorithm/transform",
        source="std",
    ),
    e(
        "std::accumulate",
        header="<numeric>",
        since="C++98",
        brief="Folds a range with a binary operation, starting from an explicit initial value. Defaults to addition.",
        signature="template<class It, class T> T accumulate(It first, It last, T init);",
        example="int total = std::accumulate(v.begin(), v.end(), 0);",
        url=_URL + "algorithm/accumulate",
        source="std",
    ),
    e(
        "std::ranges::transform",
        header="<algorithm>",
        since="C++20",
        brief="Range-based form of std::transform that accepts a range directly and integrates with the views pipeline.",
        signature="template<std::ranges::input_range R, class O, class F> auto transform(R&& r, O out, F op);",
        example="std::ranges::transform(v, out.begin(), [](int x){ return x + 1; });",
        url=_URL + "algorithm/ranges/transform",
        source="std",
    ),
    e(
        "std::ranges::filter_view",
        header="<ranges>",
        since="C++20",
        brief="Lazy view that yields only the elements of an underlying range that satisfy a predicate.",
        signature="template<std::ranges::input_range V, class Pred> class filter_view;",
        example="auto evens = v | std::views::filter([](int x){ return x % 2 == 0; });",
        url=_URL + "ranges/filter_view",
        source="std",
    ),
    e(
        "std::views::iota",
        header="<ranges>",
        since="C++20",
        brief="Generates a lazy view of consecutive incrementing values starting at a given bound.",
        signature="inline constexpr auto iota = /* unspecified */;",
        example="for (int i : std::views::iota(0, 5)) {}",
        url=_URL + "ranges/iota_view",
        source="std",
    ),
    e(
        "std::views::take",
        header="<ranges>",
        since="C++20",
        brief="Lazy view that produces the first n elements of an underlying range.",
        signature="inline constexpr auto take = /* unspecified */;",
        example="auto first3 = v | std::views::take(3);",
        url=_URL + "ranges/take_view",
        source="std",
    ),
    e(
        "std::views::drop",
        header="<ranges>",
        since="C++20",
        brief="Lazy view that skips the first n elements of an underlying range and yields the rest.",
        signature="inline constexpr auto drop = /* unspecified */;",
        example="auto tail = v | std::views::drop(2);",
        url=_URL + "ranges/drop_view",
        source="std",
    ),
    # ---------------------------------------------------------------- threading
    e(
        "std::thread",
        header="<thread>",
        since="C++11",
        brief="Owning handle for an OS thread of execution. The associated thread starts at construction and must be joined or detached before destruction.",
        signature="class thread;",
        example="std::thread t([] { /* work */ });\nt.join();",
        url=_URL + "thread/thread",
        source="std",
    ),
    e(
        "std::jthread",
        header="<thread>",
        since="C++20",
        brief="Thread that joins automatically on destruction and supports cooperative cancellation via std::stop_token.",
        signature="class jthread;",
        example="std::jthread t([](std::stop_token st) { while (!st.stop_requested()) {} });",
        url=_URL + "thread/jthread",
        source="std",
    ),
    e(
        "std::mutex",
        header="<mutex>",
        since="C++11",
        brief="Non-recursive mutual-exclusion primitive. Typically held by std::lock_guard or std::scoped_lock rather than locked directly.",
        signature="class mutex;",
        example="std::mutex m;\nstd::scoped_lock lk(m);",
        url=_URL + "thread/mutex",
        source="std",
    ),
    e(
        "std::condition_variable",
        header="<condition_variable>",
        since="C++11",
        brief="Synchronization primitive used together with a mutex to suspend threads until a predicate becomes true.",
        signature="class condition_variable;",
        example="std::condition_variable cv;\ncv.wait(lk, [&]{ return ready; });",
        url=_URL + "thread/condition_variable",
        source="std",
    ),
    e(
        "std::atomic",
        header="<atomic>",
        since="C++11",
        brief="Lock-free or lock-based wrapper that makes loads, stores, and read-modify-write operations on a value safe under concurrency.",
        signature="template<class T> struct atomic;",
        example="std::atomic<int> n{0};\nn.fetch_add(1);",
        url=_URL + "atomic/atomic",
        source="std",
    ),
    e(
        "std::future",
        header="<future>",
        since="C++11",
        brief="Receiving end of an asynchronous result. Calling .get() blocks until the producing side stores a value or exception.",
        signature="template<class T> class future;",
        example="auto f = std::async([] { return 42; });\nint x = f.get();",
        url=_URL + "thread/future",
        source="std",
    ),
    e(
        "std::promise",
        header="<future>",
        since="C++11",
        brief="Producing end of an asynchronous result. Pairs with a std::future via get_future().",
        signature="template<class T> class promise;",
        example="std::promise<int> p;\nauto f = p.get_future();\np.set_value(42);",
        url=_URL + "thread/promise",
        source="std",
    ),
    e(
        "std::async",
        header="<future>",
        since="C++11",
        brief="Runs a callable asynchronously or deferred and returns a std::future holding the eventual result.",
        signature="template<class F, class... Args> std::future<...> async(F&& f, Args&&... args);",
        example="auto f = std::async(std::launch::async, [] { return 1 + 2; });",
        url=_URL + "thread/async",
        source="std",
    ),
    e(
        "std::launch",
        header="<future>",
        since="C++11",
        brief="Bitmask policy for std::async controlling whether the task starts immediately on a new thread, is deferred, or either.",
        signature="enum class launch : /* unspecified */ { async = ..., deferred = ... };",
        example="auto f = std::async(std::launch::deferred, work);",
        url=_URL + "thread/launch",
        source="std",
    ),
    # ---------------------------------------------------------------- time
    e(
        "std::chrono::system_clock",
        header="<chrono>",
        since="C++11",
        brief="Wall-clock clock that maps to civil time and can be converted to and from std::time_t.",
        signature="class system_clock;",
        example="auto now = std::chrono::system_clock::now();",
        url=_URL + "chrono/system_clock",
        source="std",
    ),
    e(
        "std::chrono::steady_clock",
        header="<chrono>",
        since="C++11",
        brief="Monotonic clock that never goes backwards. Use it for measuring elapsed time.",
        signature="class steady_clock;",
        example="auto t0 = std::chrono::steady_clock::now();",
        url=_URL + "chrono/steady_clock",
        source="std",
    ),
    e(
        "std::chrono::duration",
        header="<chrono>",
        since="C++11",
        brief="Compile-time-checked duration value as a rep and a ratio period. std::chrono::seconds, milliseconds, etc., are aliases.",
        signature="template<class Rep, class Period = std::ratio<1>> class duration;",
        example="using namespace std::chrono_literals;\nauto d = 500ms;",
        url=_URL + "chrono/duration",
        source="std",
    ),
    e(
        "std::chrono::time_point",
        header="<chrono>",
        since="C++11",
        brief="Point in time relative to a clock's epoch, represented as a duration since that epoch.",
        signature="template<class Clock, class Duration = typename Clock::duration> class time_point;",
        example="std::chrono::system_clock::time_point now = std::chrono::system_clock::now();",
        url=_URL + "chrono/time_point",
        source="std",
    ),
    # ---------------------------------------------------------------- filesystem
    e(
        "std::filesystem::path",
        header="<filesystem>",
        since="C++17",
        brief="Portable path object with operators for joining (operator/), querying parts, and comparison.",
        signature="class path;",
        example="std::filesystem::path p = \"src\";\np /= \"main.cpp\";",
        url=_URL + "filesystem/path",
        source="std",
    ),
    e(
        "std::filesystem::exists",
        header="<filesystem>",
        since="C++17",
        brief="Returns true if the given path refers to an existing filesystem entity.",
        signature="bool exists(const std::filesystem::path& p);",
        example="if (std::filesystem::exists(\"build\")) {}",
        url=_URL + "filesystem/exists",
        source="std",
    ),
    e(
        "std::filesystem::copy",
        header="<filesystem>",
        since="C++17",
        brief="Copies files or directories, with options controlling recursion, symlink handling, and overwrite policy.",
        signature="void copy(const std::filesystem::path& from, const std::filesystem::path& to);",
        example="std::filesystem::copy(\"a.txt\", \"b.txt\");",
        url=_URL + "filesystem/copy",
        source="std",
    ),
    # ---------------------------------------------------------------- regex
    e(
        "std::regex",
        header="<regex>",
        since="C++11",
        brief="Compiled regular expression usable with std::regex_match, std::regex_search, and std::regex_replace.",
        signature="using regex = std::basic_regex<char>;",
        example="std::regex re(\"\\\\d+\");\nbool ok = std::regex_match(\"42\", re);",
        url=_URL + "regex/basic_regex",
        source="std",
    ),
    # ---------------------------------------------------------------- iostream
    e(
        "std::cout",
        header="<iostream>",
        since="C++98",
        brief="Buffered output stream connected to standard output.",
        signature="extern std::ostream cout;",
        example="std::cout << \"hello\\n\";",
        url=_URL + "io/cout",
        source="std",
    ),
    e(
        "std::cin",
        header="<iostream>",
        since="C++98",
        brief="Buffered input stream connected to standard input.",
        signature="extern std::istream cin;",
        example="int x;\nstd::cin >> x;",
        url=_URL + "io/cin",
        source="std",
    ),
    e(
        "std::cerr",
        header="<iostream>",
        since="C++98",
        brief="Unbuffered output stream connected to standard error. Each insertion flushes immediately.",
        signature="extern std::ostream cerr;",
        example="std::cerr << \"oops\\n\";",
        url=_URL + "io/cerr",
        source="std",
    ),
    e(
        "std::clog",
        header="<iostream>",
        since="C++98",
        brief="Buffered diagnostic stream connected to standard error.",
        signature="extern std::ostream clog;",
        example="std::clog << \"info\\n\";",
        url=_URL + "io/clog",
        source="std",
    ),
    e(
        "std::ifstream",
        header="<fstream>",
        since="C++98",
        brief="Input file stream for reading text or binary files.",
        signature="using ifstream = std::basic_ifstream<char>;",
        example="std::ifstream in(\"data.txt\");\nstd::string line;\nstd::getline(in, line);",
        url=_URL + "io/basic_ifstream",
        source="std",
    ),
    e(
        "std::ofstream",
        header="<fstream>",
        since="C++98",
        brief="Output file stream for writing text or binary files.",
        signature="using ofstream = std::basic_ofstream<char>;",
        example="std::ofstream out(\"log.txt\");\nout << \"hi\\n\";",
        url=_URL + "io/basic_ofstream",
        source="std",
    ),
    e(
        "std::fstream",
        header="<fstream>",
        since="C++98",
        brief="Read/write file stream that combines std::ifstream and std::ofstream behavior.",
        signature="using fstream = std::basic_fstream<char>;",
        example="std::fstream f(\"db.bin\", std::ios::in | std::ios::out | std::ios::binary);",
        url=_URL + "io/basic_fstream",
        source="std",
    ),
    e(
        "std::initializer_list",
        header="<initializer_list>",
        since="C++11",
        brief="Lightweight read-only view over a braced initializer expression. Lets a single constructor accept any number of homogeneous values.",
        signature="template<class T> class initializer_list;",
        example="void take(std::initializer_list<int> xs) {}\ntake({1, 2, 3});",
        url=_URL + "utility/initializer_list",
        source="std",
    ),
]
